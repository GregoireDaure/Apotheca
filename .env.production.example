# ─────────────────────────────────────────────────────────────────────
# Apotheca — Production Environment Variables
# Copy this file to .env.production and fill in the values
# ─────────────────────────────────────────────────────────────────────

# Domain — your server hostname (e.g., meds.example.com)
DOMAIN=meds.example.com

# Let's Encrypt email for SSL certificates
ACME_EMAIL=you@example.com

# Database credentials
DB_USER=medicine
DB_PASSWORD=CHANGE_ME_strong_random_password
DB_NAME=medicine_manager

# JWT secret — generate with: openssl rand -base64 48
JWT_SECRET=CHANGE_ME_generate_a_strong_secret

# WebAuthn
WEBAUTHN_RP_NAME=Apotheca

# VAPID keys for push notifications — generate with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
