# Database connection string
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/medicinemanager"

# External API for medicines (BDPM)
# If not set, defaults to https://open-medicaments.fr/api/v1 (which might be down)
# Alternative community API or self-hosted mirror required.
BDPM_API_URL="https://open-medicaments.fr/api/v1"

# ---- WebAuthn / Passkeys ----
JWT_SECRET="change-me-to-a-random-secret"
WEBAUTHN_RP_NAME="Apotheca"
WEBAUTHN_RP_ID="localhost"
WEBAUTHN_ORIGIN="http://localhost:5173"

# ---- Push Notifications (VAPID) ----
# Generate with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_SUBJECT="mailto:admin@apotheca.local"
